// Package registry holds the registry transport adapters implementing the
// extension.Registry port (pkg/sauron/extension), one file per transport over the
// shared primitives in registry/api.
package registry
