// Package presentation renders Sauron's command output. It is a dependency-free,
// domain-agnostic set of value types that format already-computed data; it owns
// no business logic and holds no collaborators.
package presentation
